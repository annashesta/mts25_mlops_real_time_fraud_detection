# # Auto detect text files and perform LF normalization
# * text=auto
# model/*.cbm filter=lfs diff=lfs merge=lfs -text
# *.csv filter=lfs diff=lfs merge=lfs -text
# train_data/*.csv filter=lfs diff=lfs merge=lfs -text
